name: CI

on:
  push:
    branches: [main, execution-layer, founder-alpha-rc1-roadmap]
  pull_request:
    branches: [main]

jobs:
  backend:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: decision_engine_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install ruff pytest pytest-asyncio

      - name: Lint (Ruff)
        run: ruff check . --ignore E501 F401

      - name: Run Automated Quality Gates
        env:
          JWT_SECRET: ci_fallback_super_secret_key_minimum_32_bytes
          DATABASE_URL: sqlite:///test_elite.db
        run: python scripts/verify_system.py

      - name: Test (Pytest)
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/decision_engine_test
          TEST_DATABASE_URL: sqlite:///test_elite.db
          JWT_SECRET: ci_fallback_super_secret_key_minimum_32_bytes
        run: pytest -v

  frontend:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: frontend

      - name: Build
        run: npm run build
        working-directory: frontend
